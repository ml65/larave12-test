# План разработки мини-CRM системы

## 📊 Отчет по прогрессу

| Итерация | Название | Статус | Прогресс |
|----------|----------|--------|----------|
| 1 | Настройка проекта | ✅ Выполнено | 100% |
| 2 | Модели и миграции | ✅ Выполнено | 100% |
| 3 | Базовые классы | ✅ Выполнено | 100% |
| 4 | Репозитории | ✅ Выполнено | 100% |
| 5 | Сервисы | ✅ Выполнено | 100% |
| 6 | API: создание заявок | ✅ Выполнено | 100% |
| 7 | Виджет (Blade) | ✅ Выполнено | 100% |
| 8 | Админ-панель: список | ✅ Выполнено | 100% |
| 9 | Админ-панель: детали | ✅ Выполнено | 100% |
| 10 | API: статистика | ✅ Выполнено | 100% |
| 11 | Тесты | ✅ Выполнено | 100% |
| 12 | Документация | ✅ Выполнено | 100% |

**Легенда:** ✅ Выполнено | 🚧 В работе | ⏳ Не начато | ❌ Блокировано

---

## Итерация 1: Настройка проекта

**Цель:** Создать базовую структуру Laravel проекта с необходимыми зависимостями.

- [x] Создать новый Laravel 12 проект
- [x] Установить `spatie/laravel-permission`
- [x] Установить `spatie/laravel-medialibrary`
- [x] Установить `jeroennoten/laravel-adminlte` (AdminLTE шаблон)
- [x] Настроить `.env` и `.env.example`
- [x] Настроить структуру папок (Services, Repositories, Controllers/Api, Controllers/Web)
- [x] Создать базовую конфигурацию БД

**Тест:** Проект запускается через `php artisan serve`, зависимости установлены.

---

## Итерация 2: Модели и миграции

**Цель:** Создать все модели, миграции, фабрики и сидеры с тестовыми данными.

- [x] Создать миграцию и модель `User` (обновлена, добавлен HasRoles trait)
- [x] Создать миграцию и модель `Customer` (name, phone E.164, email)
- [x] Создать миграцию и модель `Ticket` (customer_id, subject, text, status, manager_response_date)
- [x] Настроить связи между моделями
- [x] Создать фабрики для User, Customer, Ticket
- [x] Создать сидер с тестовыми данными (менеджер, клиенты, заявки)
- [x] Запустить миграции и сидеры

**Тест:** `php artisan migrate:fresh --seed` выполняется успешно, данные в БД.

---

## Итерация 3: Базовые классы

**Цель:** Создать базовые классы для наследования (BaseService, BaseRepository).

- [x] Создать `BaseService` с базовой структурой
- [x] Создать `BaseRepository` с базовыми методами CRUD
- [x] Настроить наследование

**Тест:** Базовые классы созданы, можно создать наследников.

---

## Итерация 4: Репозитории

**Цель:** Реализовать репозитории для работы с данными.

- [x] Создать `CustomerRepository` (extends BaseRepository)
- [x] Создать `TicketRepository` (extends BaseRepository)
- [x] Реализовать методы поиска, фильтрации, статистики
- [x] Добавить Eloquent scopes для статистики (дневная, недельная, месячная)

**Тест:** Репозитории работают, можно получать данные через методы.

---

## Итерация 5: Сервисы

**Цель:** Реализовать бизнес-логику в сервисах.

- [x] Создать `CustomerService` (extends BaseService)
- [x] Создать `TicketService` (extends BaseService)
- [x] Реализовать логику создания/поиска клиента (`findOrCreate`)
- [x] Реализовать логику создания заявки (`create`)
- [x] Реализовать проверку лимита (не более 1 заявки в день с одного контакта)
- [x] Реализовать логику изменения статуса заявки (`updateStatus`)

**Тест:** Сервисы работают, бизнес-логика выполняется корректно.

---

## Итерация 6: API: создание заявок

**Цель:** Реализовать API endpoint для создания заявок через виджет.

- [x] Создать `StoreTicketRequest` (FormRequest) с валидацией E.164 для телефона
- [x] Создать `TicketResource` (API Resource)
- [x] Создать `CustomerResource` (API Resource)
- [x] Создать `Api/TicketController`
- [x] Реализовать `POST /api/tickets` endpoint
- [x] Добавить обработку файлов через медиа-библиотеку
- [x] Настроить маршруты в `routes/api.php`
- [x] Добавить HasMedia trait в модель Ticket

**Тест:** Можно создать заявку через `POST /api/tickets` с валидными данными, файлы сохраняются.

---

## Итерация 7: Виджет (Blade)

**Цель:** Создать виджет для встраивания через iframe.

- [x] Создать Blade шаблон `widget.blade.php`
- [x] Реализовать форму обратной связи (имя, телефон, email, тема, текст, файлы)
- [x] Добавить стилизацию (простой CSS)
- [x] Реализовать AJAX отправку через Vanilla JavaScript
- [x] Добавить обработку ошибок и успешных отправок
- [x] Настроить маршрут `/widget` в `routes/web.php`
- [x] Создать `Web/WidgetController` для отображения виджета

**Тест:** Виджет открывается по `/widget`, форма отправляется через AJAX, работает в iframe.

---

## Итерация 8: Админ-панель: список заявок

**Цель:** Реализовать просмотр списка заявок с фильтрацией для менеджеров.

- [x] Настроить роли через `spatie/laravel-permission` (manager role уже создан в сидере)
- [x] Создать middleware для проверки роли менеджера (`EnsureUserIsManager`)
- [x] Создать `Web/TicketController` с методом `index`
- [x] Создать Blade шаблон списка заявок с использованием AdminLTE
- [x] Реализовать фильтрацию (дата, статус, email, телефон)
- [x] Добавить базовую авторизацию/аутентификацию (`Web/AuthController`)
- [x] Настроить маршруты в `routes/web.php`

**Тест:** Менеджер может авторизоваться, видит список заявок, фильтры работают.

---

## Итерация 9: Админ-панель: детали заявки

**Цель:** Реализовать просмотр деталей заявки и изменение статуса.

- [x] Создать `UpdateTicketStatusRequest` (FormRequest)
- [x] Реализовать метод просмотра деталей заявки в контроллере (`show`)
- [x] Реализовать метод изменения статуса в контроллере (`updateStatus`)
- [x] Создать Blade шаблон деталей заявки с использованием AdminLTE
- [x] Отобразить все файлы со ссылками на скачивание
- [x] Добавить форму изменения статуса

**Тест:** Менеджер видит детали заявки, файлы скачиваются, статус изменяется.

---

## Итерация 10: API: статистика

**Цель:** Реализовать API endpoint для получения статистики по заявкам.

- [x] Создать `TicketStatisticsResource` (API Resource)
- [x] Реализовать `GET /api/tickets/statistics` в `Api/TicketController`
- [x] Использовать Eloquent scopes и Carbon для расчетов (через TicketRepository)
- [x] Вернуть статистику (дневная, недельная, месячная)
- [x] Добавить проверку авторизации (только менеджеры)

**Тест:** Авторизованный менеджер получает статистику через API.

---

## Итерация 11: Тесты

**Цель:** Добавить базовые тесты для критичного функционала.

- [x] Создать тест создания заявки через API
- [x] Создать тест валидации данных (E.164, лимиты)
- [x] Создать тест изменения статуса
- [x] Создать тест статистики
- [x] Запустить все тесты, убедиться что проходят

**Тест:** `php artisan test` выполняется успешно, все тесты проходят.

---

## Итерация 12: Документация

**Цель:** Создать полную документацию проекта.

- [x] Создать `README.md` с инструкцией по запуску
- [x] Добавить список тестовых данных (логины, пароли)
- [x] Добавить пример встраивания виджета (iframe)
- [x] Добавить примеры API запросов
- [x] Создать файл с пояснениями архитектурных решений (включено в README.md)

**Тест:** Документация полная, проект можно запустить по инструкции.

---

## Дополнительно (опционально)

- [ ] Docker Compose для локального запуска
- [ ] Swagger документация API
- [ ] Дополнительные тесты

